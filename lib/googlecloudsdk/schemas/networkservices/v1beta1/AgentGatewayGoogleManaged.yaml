$schema: "http://json-schema.org/draft-06/schema#"

title: networkservices v1beta1 AgentGatewayGoogleManaged export schema
description: A gcloud export/import command YAML validation schema.
type: object
additionalProperties: false
properties:
  COMMENT:
    type: object
    description: User specified info ignored by gcloud import.
    additionalProperties: false
    properties:
      template-id:
        type: string
      region:
        type: string
      description:
        type: string
      date:
        type: string
      version:
        type: string
  UNKNOWN:
    type: array
    description: Unknown API fields that cannot be imported.
    items:
      type: string
  governedAccessPath:
    description: Operating Mode of Agent Gateway.
    type: string
    enum:
    - AGENT_TO_ANYWHERE
    - CLIENT_TO_AGENT
    - GOVERNED_ACCESS_PATH_UNSPECIFIED
