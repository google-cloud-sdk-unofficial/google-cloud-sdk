$schema: "http://json-schema.org/draft-06/schema#"

title: networkservices v1beta1 ExtensionBindingMatchConditionTo export schema
description: A gcloud export/import command YAML validation schema.
type: object
additionalProperties: false
properties:
  COMMENT:
    type: object
    description: User specified info ignored by gcloud import.
    additionalProperties: false
    properties:
      template-id:
        type: string
      region:
        type: string
      description:
        type: string
      date:
        type: string
      version:
        type: string
  UNKNOWN:
    type: array
    description: Unknown API fields that cannot be imported.
    items:
      type: string
  destination:
    description: |-
      Describes properties of destination of a request. Within a destination,
      the match follows AND semantics across fields and OR semantics within a
      field, i.e. a match occurs when ANY path matches AND ANY header matches
      and ANY method matches. At least one of destination or not_destination
      must be specified.
    $ref: ExtensionBindingMatchConditionToDestination.yaml
  notDestination:
    description: |-
      Describes the negated properties of the request destination. Extension
      will not be invoked on requests that match the criteria specified in this
      field. At least one of destination or not_destination must be specified.
    $ref: ExtensionBindingMatchConditionToDestination.yaml
