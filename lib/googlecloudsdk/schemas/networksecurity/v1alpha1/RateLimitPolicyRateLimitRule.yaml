$schema: "http://json-schema.org/draft-06/schema#"

title: networksecurity v1alpha1 RateLimitPolicyRateLimitRule export schema
description: A gcloud export/import command YAML validation schema.
type: object
additionalProperties: false
properties:
  COMMENT:
    type: object
    description: User specified info ignored by gcloud import.
    additionalProperties: false
    properties:
      template-id:
        type: string
      region:
        type: string
      description:
        type: string
      date:
        type: string
      version:
        type: string
  UNKNOWN:
    type: array
    description: Unknown API fields that cannot be imported.
    items:
      type: string
  from_:
    description: Describes properties of a source of a request.
    $ref: RateLimitPolicyRateLimitRuleFrom.yaml
  rateLimitActions:
    description: Specifies the actions to take when this rule is matched.
    type: array
    items:
      $ref: RateLimitPolicyRateLimitRuleRateLimitAction.yaml
  to:
    description: Describes properties of a target of a request.
    $ref: RateLimitPolicyRateLimitRuleTo.yaml
