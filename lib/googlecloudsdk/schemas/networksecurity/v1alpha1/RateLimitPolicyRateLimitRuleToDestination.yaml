$schema: "http://json-schema.org/draft-06/schema#"

title: networksecurity v1alpha1 RateLimitPolicyRateLimitRuleToDestination export schema
description: A gcloud export/import command YAML validation schema.
type: object
additionalProperties: false
properties:
  COMMENT:
    type: object
    description: User specified info ignored by gcloud import.
    additionalProperties: false
    properties:
      template-id:
        type: string
      region:
        type: string
      description:
        type: string
      date:
        type: string
      version:
        type: string
  UNKNOWN:
    type: array
    description: Unknown API fields that cannot be imported.
    items:
      type: string
  headerSet:
    description: Specifies a list of headers to match against in http header.
    $ref: RateLimitPolicyRateLimitRuleToDestinationHeaderSet.yaml
  hosts:
    description: |-
      Specifies a list of HTTP Hosts to match against. The match can be one of
      exact, prefix, suffix, or contains (substring match). Matches are always
      case sensitive unless the ignoreCase is set. The match follows OR
      semantics which means that if any of the hosts match, the operation is
      considered to be matched.
    type: array
    items:
      $ref: RateLimitPolicyRateLimitRuleStringMatch.yaml
  mcp:
    description: |-
      Specifies the MCP protocol attributes to match against. This field is
      only valid if the targeted Gateway or Forwarding Rule has an Agent
      Gateway attached to it.
    $ref: RateLimitPolicyRateLimitRuleToDestinationMCP.yaml
  methods:
    description: |-
      Specifies a list of HTTP methods to match against. Each entry must be
      a valid HTTP method name (GET, PUT, POST, HEAD, PATCH, DELETE,
      OPTIONS). It only allows exact match and is always case sensitive. The
      match follows OR semantics which means that if any of the methods
      match, the operation is considered to be matched.
    type: array
    items:
      type: string
  paths:
    description: |-
      Specifies a list of paths to match against. The match can be one
      of exact, prefix, suffix, or contains (substring match). Matches
      are always case sensitive unless the ignoreCase is set. The match
      follows OR semantics which means that if any of the paths match,
      the operation is considered to be matched. Note that this path
      match includes the query parameters. For gRPC services, this
      should be a fully-qualified name of the form
      /package.service/method.
    type: array
    items:
      $ref: RateLimitPolicyRateLimitRuleStringMatch.yaml
