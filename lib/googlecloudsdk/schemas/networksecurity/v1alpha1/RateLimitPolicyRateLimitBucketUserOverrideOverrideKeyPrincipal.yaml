$schema: "http://json-schema.org/draft-06/schema#"

title: networksecurity v1alpha1 RateLimitPolicyRateLimitBucketUserOverrideOverrideKeyPrincipal
  export schema
description: A gcloud export/import command YAML validation schema.
type: object
required:
- principal
- principalType
additionalProperties: false
properties:
  COMMENT:
    type: object
    description: User specified info ignored by gcloud import.
    additionalProperties: false
    properties:
      template-id:
        type: string
      region:
        type: string
      description:
        type: string
      date:
        type: string
      version:
        type: string
  UNKNOWN:
    type: array
    description: Unknown API fields that cannot be imported.
    items:
      type: string
  principal:
    description: Specifies the principal value of the key.
    type: string
  principalType:
    description: Specifies the principal type of the key.
    type: string
    enum:
    - CLIENT_CERT_COMMON_NAME
    - PRINCIPAL_TYPE_UNSPECIFIED
