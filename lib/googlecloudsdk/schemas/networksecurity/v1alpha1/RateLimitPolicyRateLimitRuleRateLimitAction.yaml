$schema: "http://json-schema.org/draft-06/schema#"

title: networksecurity v1alpha1 RateLimitPolicyRateLimitRuleRateLimitAction export
  schema
description: A gcloud export/import command YAML validation schema.
type: object
required:
- rateLimitBucket
additionalProperties: false
properties:
  COMMENT:
    type: object
    description: User specified info ignored by gcloud import.
    additionalProperties: false
    properties:
      template-id:
        type: string
      region:
        type: string
      description:
        type: string
      date:
        type: string
      version:
        type: string
  UNKNOWN:
    type: array
    description: Unknown API fields that cannot be imported.
    items:
      type: string
  rateLimitBucket:
    description: |-
      Specifies the name of the rate limit bucket to apply when this rule is
      matched.
    type: string
