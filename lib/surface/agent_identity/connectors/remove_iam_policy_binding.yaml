- release_tracks: [ALPHA]
  help_text:
    brief: Remove IAM policy binding from an Agent Identity connector.
    description: |
      Remove an IAM policy binding from the IAM policy of an Agent Identity connector. One binding consists of a member,
      and a role.

    examples: |
      To remove an IAM policy binding for the role of 'roles/iamconnectors.user' for the user 'test-user@gmail.com'
      with connector 'my-connector' and location 'us-central1', run:

        $ {command} my-connector --location='us-central1' --member='user:test-user@gmail.com' --role='roles/iamconnectors.user'

  request:
    api_version: v1alpha
    collection: iamconnectors.projects.locations.connectors

  arguments:
    resource:
      help_text: The connector from which to remove the IAM policy binding.
      spec: !REF googlecloudsdk.command_lib.agent_identity.v1alpha_resources:projects_locations_connectors

  iam:
    enable_condition: false
