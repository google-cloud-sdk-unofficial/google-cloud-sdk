# -*- coding: utf-8 -*- #
# Copyright 2026 Google LLC. All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#    http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# NOTE: This file is autogenerated and should not be edited by hand.
# AUTOGEN_CLI_VERSION: HEAD
- release_tracks:
  - BETA
  auto_generated: true
  help_text:
    brief: Create an auth provider.
    description: Create a new auth provider in a given project and location.
    examples: |-
      To create an API key auth provider 'my-auth-provider' in location 'us-central1' and project 'my-project', run:

        $ {command} my-auth-provider --location=us-central1 --project=my-project --api-key=my-api-key
  arguments:
    params:
    - help_text: |-
        Identifier. The full resource name of the auth_provider. Format:
        projects/{project}/locations/{location}/authProviders/{auth_provider}
      is_positional: true
      is_primary_resource: true
      request_id_field: authProviderId
      resource_spec: !REF googlecloudsdk.command_lib.agent_identity.authprovider_v1alpha_resources:projects_locations_auth_providers
      required: true
    - arg_name: labels
      api_field: authProvider.labels
      required: false
      repeated: true
      help_text: |-
        Labels as key value pairs
      spec:
      - api_field: key
      - api_field: value
    - group:
        api_field: authProvider.authProviderTypeParams
        required: true
        help_text: |-
          AuthProvider type specific parameters.
          Required when creating an auth_provider.
        params:
        - group:
            mutex: true
            help_text: |-
              Arguments for the type.
            params:
            - group:
                api_field: authProvider.authProviderTypeParams.threeLeggedOauth
                required: false
                help_text: |-
                  Message describing ThreeLeggedOAuth object.
                params:
                - arg_name: three-legged-oauth-client-secret
                  api_field: authProvider.authProviderTypeParams.threeLeggedOauth.clientSecret
                  required: false
                  repeated: false
                  help_text: |-
                    The client secret of the OAuth client.
                - arg_name: three-legged-oauth-client-id
                  api_field: authProvider.authProviderTypeParams.threeLeggedOauth.clientId
                  required: false
                  repeated: false
                  help_text: |-
                    The client ID of the OAuth client.
                - arg_name: three-legged-oauth-authorization-url
                  api_field: authProvider.authProviderTypeParams.threeLeggedOauth.authorizationUrl
                  required: false
                  repeated: false
                  help_text: |-
                    The authorization endpoint to send users to for consenting to delegate
                    to the agent.
                    eg. "https://auth.atlassian.com/authorize"
                - arg_name: three-legged-oauth-token-url
                  api_field: authProvider.authProviderTypeParams.threeLeggedOauth.tokenUrl
                  required: false
                  repeated: false
                  help_text: |-
                    The token endpoint for requesting tokens on behalf of an end user.
                    eg. "https://auth.atlassian.com/oauth/token"
                - arg_name: three-legged-oauth-enable-pkce
                  api_field: authProvider.authProviderTypeParams.threeLeggedOauth.enablePkce
                  action: store_true
                  required: false
                  type: bool
                  help_text: |-
                    Enables Proof Key for Code Exchange (PKCE) for the OAuth flow to prevent
                    authorization code interception attacks.
                  default: null
            - group:
                api_field: authProvider.authProviderTypeParams.twoLeggedOauth
                required: false
                help_text: |-
                  Message describing TwoLeggedOAuth object.
                params:
                - arg_name: two-legged-oauth-client-secret
                  api_field: authProvider.authProviderTypeParams.twoLeggedOauth.clientSecret
                  required: false
                  repeated: false
                  help_text: |-
                    The client secret of the OAuth client.
                - arg_name: two-legged-oauth-client-id
                  api_field: authProvider.authProviderTypeParams.twoLeggedOauth.clientId
                  required: false
                  repeated: false
                  help_text: |-
                    The client ID of the OAuth client.
                - arg_name: two-legged-oauth-token-url
                  api_field: authProvider.authProviderTypeParams.twoLeggedOauth.tokenUrl
                  required: false
                  repeated: false
                  help_text: |-
                    The token endpoint of the OAuth client.
            - group:
                api_field: authProvider.authProviderTypeParams.apiKey
                required: false
                help_text: |-
                  Message describing ApiKeyParams object.
                params:
                - arg_name: api-key
                  api_field: authProvider.authProviderTypeParams.apiKey.apiKey
                  required: false
                  repeated: false
                  help_text: |-
                    The API key for this auth_provider.
            - group:
                api_field: authProvider.authProviderTypeParams.geAuthProvider
                arg_name: auth-provider-type-params-ge
                settable: true
                required: false
                hidden: true
                help_text: |-
                  Message describing GeminiEnterpriseAuthProviderParams object.
                params: []
    - arg_name: allowed-scopes
      api_field: authProvider.allowedScopes
      required: false
      repeated: true
      help_text: |-
        List of scopes that are allowed to be requested for this auth_provider.
        If this list is non-empty, only scopes within this list may be requested.
        If this list is empty, all scopes may be requested.
        Scopes appearing in `blocked_scopes` are disallowed even if they appear in
        `allowed_scopes`.
        The number of allowed scopes is limited to 200.
    - arg_name: blocked-scopes
      api_field: authProvider.blockedScopes
      required: false
      repeated: true
      help_text: |-
        List of scopes that are blocked from being requested for this
        auth_provider. If a scope appears in this list, it will not be requested,
        even if it also appears in `allowed_scopes`. `blocked_scopes` takes
        precedence over `allowed_scopes`. The number of blocked scopes is limited
        to 200.
    - arg_name: description
      api_field: authProvider.description
      required: false
      repeated: false
      help_text: |-
        Description of the resource.
        Must be less than 256 characters.
    - arg_name: workload-ids
      api_field: authProvider.workloadIds
      required: false
      repeated: true
      help_text: |-
        Represents the workload identity in IAM `principal://` format of the
        agent(s) that will use this AuthProvider. Example:
        `principal://agents.global.org-${ORG_ID}.system.id.goog/resources/aiplatform/projects/{PROJECT_ID}/locations/{LOCATIONS}/reasoningEngines/{ID}`
    - arg_name: request-id
      api_field: requestId
      required: false
      repeated: false
      help_text: |-
        An optional request ID to identify requests. Specify a unique request ID
        so that if you must retry your request, the server will know to ignore
        the request if it has already been completed. The server will guarantee
        that for at least 60 minutes since the first request.

        For example, consider a situation where you make an initial request and the
        request times out. If you make the request again with the same request
        ID, the server can check if original operation with the same request ID
        was received, and if so, will ignore the second request. This prevents
        clients from accidentally creating duplicate commitments.

        The request ID must be a valid UUID with the exception that zero UUID is
        not supported (00000000-0000-0000-0000-000000000000).
  request:
    api_version: v1alpha
    collection:
    - agentidentity.projects.locations.authProviders
