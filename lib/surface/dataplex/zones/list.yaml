- release_tracks: [ALPHA]
  hidden: true
  help_text:
    brief: |
      List Dataplex Zone Resources.
    description: |
      List Dataplex Zone resource based on project, location, and lake.

      {top_command} dataplex zone list projects/{project_id}/locations/{location}/lakes/{lake_id}
    examples: |
      {top_command} list --projects="test-project" --locations=us-central1 --lakes=test-lake

  request:
    ALPHA:
      api_version: v1
    collection: dataplex.projects.locations.lakes.zones
  arguments:
    resource:
      help_text: |
        Arguments and flags that define the Dataplex Zone you want to list.
      spec: !REF googlecloudsdk.command_lib.dataplex.resources:lake
