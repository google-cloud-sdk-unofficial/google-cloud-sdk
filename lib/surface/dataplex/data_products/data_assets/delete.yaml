- release_tracks: [ALPHA]
  hidden: true
  help_text:
    brief: |
      Delete a Dataplex data asset resource.
    description: |
      Delete a Dataplex data asset resource.
    examples: |
      To delete a Dataplex data asset `test-da` in data product `test-dp` in location `us-central1`, run:

          $ {command} test-da --data-product=test-dp --location=us-central1

  request:
    ALPHA:
      api_version: v1
    collection: dataplex.projects.locations.dataProducts.dataAssets
  arguments:
    resource:
      help_text: |
        Arguments and flags that specify the Dataplex Data Asset you want to delete.
      spec: !REF googlecloudsdk.command_lib.dataplex.resources:data_asset
    params:
    - arg_name: etag
      api_field: etag
      help_text: |
        Etag of the data asset to delete.
  async:
    collection: dataplex.projects.locations.operations
