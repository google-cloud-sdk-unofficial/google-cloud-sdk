- release_tracks: [ALPHA]
  hidden: true
  help_text:
    brief: |
      Get a Dataplex Data Product resource.
    description: |
      Get a Dataplex Data Product resource based on project, location and data product id.

      {command} projects/{project_id}/locations/{location_id}/dataProducts/{data_product_id}
    examples: |
      $ {command} projects/test-project/locations/us-central1/dataProducts/test-dp
  request:
    ALPHA:
      api_version: v1
    collection: dataplex.projects.locations.dataProducts
    method: get
  arguments:
    resource:
      help_text: |
        Arguments and flags that specify the Dataplex Data Product you want to get.
      spec: !REF googlecloudsdk.command_lib.dataplex.resources:data_product
