- release_tracks: [ALPHA, BETA]
  help_text:
    brief: Remove an IAM policy binding from a Cloud Spanner instance.
    DESCRIPTION: |
      Remove an IAM policy binding from a Cloud Spanner instance.
  request:
    collection: spanner.projects.instances

  arguments:
    resource:
      help_text: The Cloud Spanner instance to remove the IAM policy binding from.
      spec: !REF googlecloudsdk.command_lib.spanner.resources:instance
