- release_tracks: [ALPHA, BETA]
  help_text:
    brief: Remove an IAM policy binding from a Cloud Spanner database.
    DESCRIPTION: |
      Remove an IAM policy binding from a Cloud Spanner database.
  request:
    collection: spanner.projects.instances.databases

  arguments:
    resource:
      help_text: The Cloud Spanner database to remove the IAM policy binding from.
      spec: !REF googlecloudsdk.command_lib.spanner.resources:database
