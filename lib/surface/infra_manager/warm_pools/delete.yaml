- release_tracks: [ALPHA]
  hidden: true
  help_text:
    brief: Delete a warm pool.
    description: Delete a warm pool.
    examples: |-
      To delete a warm pool `my-warm-pool` at location `us-central1`, run:

        $ {command} my-warm-pool --location=us-central1

  request:
    collection: config.projects.locations.warmPools

  arguments:
    resource:
      spec: !REF googlecloudsdk.command_lib.infra_manager.resources:warm_pool
      help_text: The warm pool to delete.

  async:
    collection: config.projects.locations.operations
