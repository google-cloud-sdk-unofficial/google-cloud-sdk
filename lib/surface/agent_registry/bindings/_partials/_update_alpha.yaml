# -*- coding: utf-8 -*- #
# Copyright 2026 Google LLC. All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#    http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# NOTE: This file is autogenerated and should not be edited by hand.
# AUTOGEN_CLI_VERSION: HEAD
- release_tracks:
  - ALPHA
  auto_generated: true
  help_text:
    brief: Update bindings
    description: Update a binding
    examples: |-
      To update the binding, run:

          $ {command}
  arguments:
    params:
    - group:
        mutex: true
        help_text: |-
          Arguments for the binding.
        params:
        - group:
            api_field: binding.authProviderBinding
            arg_name: auth-provider-binding
            clearable: true
            required: false
            help_text: |-
              The AuthProvider of the Binding.
            params:
            - arg_name: auth-provider-binding
              api_field: binding.authProviderBinding.authProvider
              required: false
              repeated: false
              help_text: |-
                The resource name of the target AuthProvider.
                Format:

                * `projects/{project}/locations/{location}/authProviders/{auth_provider}`
            - arg_name: auth-provider-binding-scopes
              api_field: binding.authProviderBinding.scopes
              required: false
              repeated: true
              help_text: |-
                The list of OAuth2 scopes of the AuthProvider.
              clearable: true
            - arg_name: auth-provider-binding-continue-uri
              api_field: binding.authProviderBinding.continueUri
              required: false
              repeated: false
              help_text: |-
                The continue URI of the AuthProvider.
                The URI is used to reauthenticate the user and finalize the managed OAuth
                flow.
    - help_text: |-
        Identifier. The resource name of the Binding.
        Format: `projects/{project}/locations/{location}/bindings/{binding}`.
      is_positional: true
      is_primary_resource: true
      resource_spec: !REF googlecloudsdk.command_lib.agent_registry.v1alpha_resources:projects_locations_bindings
      required: true
    - arg_name: display-name
      api_field: binding.displayName
      required: false
      repeated: false
      help_text: |-
        User-defined display name for the Binding.
        Can have a maximum length of `63` characters.
    - arg_name: description
      api_field: binding.description
      required: false
      repeated: false
      help_text: |-
        User-defined description of a Binding.
        Can have a maximum length of `2048` characters.
    - group:
        api_field: binding.source
        arg_name: source
        clearable: true
        required: false
        help_text: |-
          The source of the Binding.
        params:
        - group:
            mutex: true
            help_text: |-
              Arguments for the source type.
            params:
            - arg_name: source-identifier
              api_field: binding.source.identifier
              required: false
              repeated: false
              help_text: |-
                The identifier of the source Agent.
                Format:

                * `urn:agent:{publisher}:{namespace}:{name}`
    - group:
        api_field: binding.target
        arg_name: target
        clearable: true
        required: false
        help_text: |-
          The target of the Binding.
        params:
        - group:
            mutex: true
            help_text: |-
              Arguments for the target type.
            params:
            - arg_name: target-identifier
              api_field: binding.target.identifier
              required: false
              repeated: false
              help_text: |-
                The identifier of the target Agent, MCP Server, or Endpoint.
                Format:

                * `urn:agent:{publisher}:{namespace}:{name}`
                * `urn:mcp:{publisher}:{namespace}:{name}`
                * `urn:endpoint:{publisher}:{namespace}:{name}`
    - arg_name: request-id
      api_field: requestId
      required: false
      repeated: false
      help_text: |-
        An optional request ID to identify requests. Specify a unique request ID
        so that if you must retry your request, the server will know to ignore
        the request if it has already been completed. The server will guarantee
        that for at least 60 minutes since the first request.

        For example, consider a situation where you make an initial request and the
        request times out. If you make the request again with the same request
        ID, the server can check if original operation with the same request ID
        was received, and if so, will ignore the second request. This prevents
        clients from accidentally creating duplicate commitments.

        The request ID must be a valid UUID with the exception that zero UUID is
        not supported (00000000-0000-0000-0000-000000000000).
  request:
    api_version: v1alpha
    collection:
    - agentregistry.projects.locations.bindings
  async:
    collection:
    - agentregistry.projects.locations.operations
  update:
    read_modify_update: true
