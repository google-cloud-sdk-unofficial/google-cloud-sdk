# -*- coding: utf-8 -*- #
# Copyright 2025 Google LLC. All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#    http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# NOTE: This file is autogenerated and should not be edited by hand.
# AUTOGEN_CLI_VERSION: HEAD
- release_tracks:
  - ALPHA
  auto_generated: true
  help_text:
    brief: Create services
    description: Create a service
    examples: |-
      To create the service, run:

          $ {command}
  arguments:
    params:
    - group:
        mutex: true
        help_text: |-
          Arguments for the spec.
        params:
        - group:
            api_field: service.agentSpec
            required: false
            help_text: |-
              The spec of the agent.
            params:
            - arg_name: agent-spec-type
              api_field: service.agentSpec.type
              required: true
              help_text: |-
                The type of the agent spec content.
              choices:
              - arg_value: no-spec
                enum_value: NO_SPEC
                help_text: |-
                  There is no spec for the Agent. The content field must be empty.
              - arg_value: a2a-agent-card
                enum_value: A2A_AGENT_CARD
                help_text: |-
                  The content is an A2A Agent Card following the A2A specification.
                  The interfaces field must be empty.
            - arg_name: agent-spec-content
              api_field: service.agentSpec.content
              required: false
              repeated: false
              type: arg_json
              help_text: |-
                The content of the Agent spec in the JSON format.
                This payload is validated against the schema for the specified type.
        - group:
            api_field: service.mcpServerSpec
            required: false
            help_text: |-
              The spec of the MCP Server.
            params:
            - arg_name: mcp-server-spec-type
              api_field: service.mcpServerSpec.type
              required: true
              help_text: |-
                The type of the MCP Server spec content.
              choices:
              - arg_value: no-spec
                enum_value: NO_SPEC
                help_text: |-
                  There is no spec for the MCP Server. The content field must be empty.
              - arg_value: tool-spec
                enum_value: TOOL_SPEC
                help_text: |-
                  The content is a MCP Tool Spec following the One MCP specification.
                  The payload is the same as the tools/list response.
            - arg_name: mcp-server-spec-content
              api_field: service.mcpServerSpec.content
              required: false
              repeated: false
              type: arg_json
              help_text: |-
                The content of the MCP Server spec.
                This payload is validated against the schema for the specified type.
        - group:
            api_field: service.endpointSpec
            required: false
            help_text: |-
              The spec of the endpoint.
            params:
            - arg_name: endpoint-spec-type
              api_field: service.endpointSpec.type
              required: true
              help_text: |-
                The type of the endpoint spec content.
              choices:
              - arg_value: no-spec
                enum_value: NO_SPEC
                help_text: |-
                  There is no spec for the Endpoint. The content field must be empty.
            - arg_name: endpoint-spec-content
              api_field: service.endpointSpec.content
              required: false
              repeated: false
              type: arg_json
              help_text: |-
                The content of the endpoint spec.
                Reserved for future use.
    - help_text: |-
        Identifier. The resource name of the Service.
        Format: `projects/{project}/locations/{location}/services/{service}`.
      is_positional: true
      is_primary_resource: true
      request_id_field: serviceId
      resource_spec: !REF googlecloudsdk.command_lib.agent_registry.v1alpha_resources:projects_locations_services
      required: true
    - arg_name: display-name
      api_field: service.displayName
      required: false
      repeated: false
      help_text: |-
        User-defined display name for the Service.
        Can have a maximum length of 63 characters.
    - arg_name: description
      api_field: service.description
      required: false
      repeated: false
      help_text: |-
        User-defined description of an Service.
        Can have a maximum length of 2048 characters.
    - arg_name: interfaces
      api_field: service.interfaces
      required: true
      repeated: true
      help_text: |-
        The connection details for the Service.
      spec:
      - api_field: url
        help_text: |-
          The destination URL.
      - api_field: protocolBinding
        help_text: |-
          The protocol binding of the interface.
    - arg_name: request-id
      api_field: requestId
      required: false
      repeated: false
      help_text: |-
        An optional request ID to identify requests. Specify a unique request ID
        so that if you must retry your request, the server will know to ignore
        the request if it has already been completed. The server will guarantee
        that for at least 60 minutes since the first request.

        For example, consider a situation where you make an initial request and the
        request times out. If you make the request again with the same request
        ID, the server can check if original operation with the same request ID
        was received, and if so, will ignore the second request. This prevents
        clients from accidentally creating duplicate commitments.

        The request ID must be a valid UUID with the exception that zero UUID is
        not supported (00000000-0000-0000-0000-000000000000).
  request:
    api_version: v1alpha
    collection:
    - agentregistry.projects.locations.services
  async:
    collection:
    - agentregistry.projects.locations.operations
