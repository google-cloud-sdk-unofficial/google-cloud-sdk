# -*- coding: utf-8 -*- #
# Copyright 2026 Google LLC. All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#    http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# NOTE: This file is autogenerated and should not be edited by hand.
# AUTOGEN_CLI_VERSION: HEAD
- release_tracks:
  - ALPHA
  auto_generated: false
  hidden: true
  help_text:
    brief: Create skills
    description: Create a skill
    examples: |-
      To create the skill, run:

          $ {command}
  arguments:
    params:
    - help_text: |-
        Identifier. Resource name of the Skill.
        Format: `projects/{project}/locations/{location}/skills/{skill}`
        The `{skill}` segment acts as the resource ID. If the skill is associated
        with a Publisher, this segment typically uses a hyphenated namespace prefix
        corresponding to the publisher (e.g., `google-workspace-create-docs`).
      is_positional: true
      is_primary_resource: true
      request_id_field: skillId
      resource_spec: !REF googlecloudsdk.command_lib.agent_registry.v1alpha_resources:projects_locations_skills
      required: true
    - arg_name: display-name
      api_field: skill.displayName
      required: false
      repeated: false
      help_text: |-
        Human-readable display name of the skill.
        Maximum length is 128 characters.
    - arg_name: description
      api_field: skill.description
      required: false
      repeated: false
      help_text: |-
        Brief summary describing the capabilities of the skill.
        Maximum length is 2048 characters.
    - arg_name: type
      api_field: skill.type
      required: false
      help_text: |-
        Structural deployment type (SIMPLE leaf vs COMPOSITE bundle).
      choices:
      - arg_value: simple
        enum_value: SIMPLE
        help_text: |-
          A standard, leaf executable skill containing natural language and code.
    - arg_name: target-state
      api_field: skill.targetState
      required: false
      help_text: |-
        User-managed target state of the skill.
      choices:
      - arg_value: target-state-draft
        enum_value: TARGET_STATE_DRAFT
        help_text: |-
          The skill is in draft and cannot be served.
      - arg_value: target-state-active
        enum_value: TARGET_STATE_ACTIVE
        help_text: |-
          The skill is active and can be served.
      - arg_value: target-state-disabled
        enum_value: TARGET_STATE_DISABLED
        help_text: |-
          The skill is disabled and cannot be served.
      - arg_value: target-state-deprecated
        enum_value: TARGET_STATE_DEPRECATED
        help_text: |-
          The skill is deprecated. It can still be served, but emits warnings.
      - arg_value: target-state-decommissioned
        enum_value: TARGET_STATE_DECOMMISSIONED
        help_text: |-
          The skill is decommissioned and cannot be served.
      - arg_value: draft
        enum_value: TARGET_STATE_DRAFT
        help_text: The skill is in draft.
      - arg_value: active
        enum_value: TARGET_STATE_ACTIVE
        help_text: The skill is active.
      - arg_value: disabled
        enum_value: TARGET_STATE_DISABLED
        help_text: The skill is disabled.
      - arg_value: deprecated
        enum_value: TARGET_STATE_DEPRECATED
        help_text: The skill is deprecated.
      - arg_value: decommissioned
        enum_value: TARGET_STATE_DECOMMISSIONED
        help_text: The skill is decommissioned.
      - arg_value: DRAFT
        enum_value: TARGET_STATE_DRAFT
        help_text: The skill is in draft.
      - arg_value: ACTIVE
        enum_value: TARGET_STATE_ACTIVE
        help_text: The skill is active.
      - arg_value: DISABLED
        enum_value: TARGET_STATE_DISABLED
        help_text: The skill is disabled.
      - arg_value: DEPRECATED
        enum_value: TARGET_STATE_DEPRECATED
        help_text: The skill is deprecated.
      - arg_value: DECOMMISSIONED
        enum_value: TARGET_STATE_DECOMMISSIONED
        help_text: The skill is decommissioned.
    - arg_name: default-revision
      help_text: |-
        The full resource name of the revision currently served by
        default (floating track).
        Format:
        `projects/{project}/locations/{location}/skills/{skill}/revisions/{revision}`
      is_positional: false
      is_primary_resource: false
      resource_spec: !REF googlecloudsdk.command_lib.agent_registry.v1alpha_resources:projects_locations_skills_revisions
      resource_method_params:
        skill.defaultRevision: '{__relative_name__}'
      required: false
      removed_flags: [skill]
    - arg_name: publisher
      help_text: |-
        The publisher resource associated with this skill.
        Format: `projects/{project}/locations/{location}/publishers/{publisher}`
        The publisher dictates the allowed namespace prefixes for the skill's name
        and logical `skill_id` (e.g., Publisher `google` authorizes the `google-*`
        prefix).
      is_positional: false
      is_primary_resource: false
      resource_spec: !REF googlecloudsdk.command_lib.agent_registry.v1alpha_resources:projects_locations_publishers
      resource_method_params:
        skill.publisher: '{__relative_name__}'
      required: false
    - group:
        api_field: skill.initialRevision
        required: false
        help_text: |-
          Represents an immutable, versioned snapshot of a Skill package.
        params:
        - group:
            mutex: true
            help_text: |-
              Arguments for the source.
            params:
            - group:
                api_field: skill.initialRevision.gcsSource
                required: false
                help_text: |-
                  Specifications for Cloud Storage objects.
                params:
                - arg_name: gcs-source-uri
                  api_field: skill.initialRevision.gcsSource.uri
                  required: true
                  repeated: false
                  help_text: |-
                    Cloud Storage object URI.
                    Format:
                    `gs://{bucket_name}/{object_name}`
                - arg_name: gcs-source-generation
                  api_field: skill.initialRevision.gcsSource.generation
                  required: false
                  repeated: false
                  type: long
                  help_text: |-
                    Cloud Storage object generation ID. If not specified, the latest
                    generation is used.
            - group:
                api_field: skill.initialRevision.archiveUploadSource
                required: false
                help_text: |-
                  Direct write-only raw archive payload upload.
                params:
                - arg_name: payload
                  required: true
                  type: str
                  help_text: |-
                    Local path to the ZIP archive payload containing the skill.
        - arg_name: initial-revision-name
          api_field: skill.initialRevision.name
          required: false
          repeated: false
          help_text: |-
            Identifier. Resource name of the SkillRevision.
            Format:
            `projects/{project}/locations/{location}/skills/{skill}/revisions/{revision}`
    - arg_name: request-id
      api_field: requestId
      required: false
      repeated: false
      help_text: |-
        Signed UUID request idempotency token.
  request:
    api_version: v1alpha
    collection:
    - agentregistry.projects.locations.skills
    modify_request_hooks:
    - googlecloudsdk.command_lib.agent_registry.util:StripPrivatePrefixHook
    - googlecloudsdk.command_lib.agent_registry.util:SetDisplayNameAndDefaultsInCreate
  response:
    modify_response_hooks:
    - googlecloudsdk.command_lib.agent_registry.util:EnsurePrivatePrefixInResponseHook
  async:
    collection:
    - agentregistry.projects.locations.operations
    extract_resource_result: false
    result_attribute: response
