- release_tracks:
  - ALPHA
  hidden: true
  help_text:
    brief: Delete Flag Attribute
    description: Delete a Flag Attribute
    examples: |-
      To delete a Flag Attribute, run:

      $ {command}
  arguments:
    params:
    - help_text: |-
        Flag Attribute ID of the Flag Attribute to delete.
      is_positional: true
      resource_spec: !REF googlecloudsdk.command_lib.saas_runtime.resources:flag_attribute
      required: true
  request:
    api_version: v1beta1
    collection:
    - saasservicemgmt.projects.locations.flagAttributes
