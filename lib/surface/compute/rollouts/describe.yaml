- release_tracks: [ALPHA, BETA]
  help_text:
    brief: "Describe a rollout."
    description: "Describe a rollout."
    examples: |
      To retrieve a single rollout and print its properties, run the following command:

      $ {command} my-rollout

  request:
    ALPHA:
      api_version: alpha
    BETA:
      api_version: beta

    collection: compute.rollouts

  arguments:
    resource:
      help_text: Name of the rollout you want to inspect.
      spec: !REF googlecloudsdk.command_lib.compute.resources:rollout
