# -*- coding: utf-8 -*- #
# Copyright 2025 Google LLC. All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#    http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# NOTE: This file is autogenerated and should not be edited by hand.
# AUTOGEN_CLI_VERSION: HEAD
- release_tracks:
  - ALPHA
  auto_generated: true
  hidden: true
  help_text:
    brief: Create a custom range resource
    description: |
      Create a custom range resource.
    examples: |-
      To create a custom range `my-custom-range` run:

      $ {command} my-custom-range \
        --location=global \
        --realm=my-realm \
        --ipv4-cidr-range=10.0.0.0/24 \
  arguments:
    params:
    - group:
        mutex: true
        help_text: |-
          Arguments for the ip cidr range.
        params:
        - arg_name: ipv4-cidr-range
          api_field: customRange.ipv4CidrRange
          required: false
          repeated: false
          help_text: |-
            The IPv4 CIDR range of the CustomRange.
        - arg_name: ipv6-cidr-range
          api_field: customRange.ipv6CidrRange
          required: false
          repeated: false
          help_text: |-
            The IPv6 CIDR range of the CustomRange.
    - help_text: |-
        Identifier. name of resource
      is_positional: true
      is_primary_resource: true
      request_id_field: customRangeId
      resource_spec: !REF googlecloudsdk.command_lib.number_registry.v1alpha_resources:projects_locations_custom_ranges
      required: true
    - arg_name: realm
      help_text: |-
        The realm of the CustomRange. The realm must be in the same project as the
        custom range. Do not allow setting realm if parent range is specified,
        since the realm should be inherited from the parent range. Format must
        follow this pattern: projects/{project}/locations/{location}/realms/{realm}
      is_positional: false
      is_primary_resource: false
      resource_spec: !REF googlecloudsdk.command_lib.number_registry.v1alpha_resources:projects_locations_realms
      resource_method_params:
        customRange.realm: '{__relative_name__}'
      required: false
    - arg_name: parent-range
      help_text: |-
        The parent range of the CustomRange. Do not allow setting parent range if
        realm is specified. Format must follow this pattern:
        projects/{project}/locations/{location}/customRanges/{custom_range}
      is_positional: false
      is_primary_resource: false
      resource_spec: !REF googlecloudsdk.command_lib.number_registry.v1alpha_resources:projects_locations_custom_ranges
      resource_method_params:
        customRange.parentRange: '{__relative_name__}'
      required: false
    - arg_name: attributes
      api_field: customRange.attributes
      required: false
      repeated: true
      help_text: |-
        The attributes of the CustomRange.
      spec:
      - api_field: key
        help_text: |-
          Key of attribute
      - api_field: value
        help_text: |-
          Value of attribute
    - arg_name: description
      api_field: customRange.description
      required: false
      repeated: false
      help_text: |-
        Description of the CustomRange.
    - arg_name: labels
      api_field: customRange.labels
      required: false
      repeated: true
      help_text: |-
        Labels as key value pairs
      spec:
      - api_field: key
      - api_field: value
    - arg_name: request-id
      api_field: requestId
      required: false
      repeated: false
      help_text: |-
        An optional request ID to identify requests. Specify a unique request ID
        so that if you must retry your request, the server will know to ignore
        the request if it has already been completed. The server will guarantee
        that for at least 60 minutes since the first request.

        For example, consider a situation where you make an initial request and the
        request times out. If you make the request again with the same request
        ID, the server can check if original operation with the same request ID
        was received, and if so, will ignore the second request. This prevents
        clients from accidentally creating duplicate commitments.

        The request ID must be a valid UUID with the exception that zero UUID is
        not supported (00000000-0000-0000-0000-000000000000).
  request:
    api_version: v1alpha
    collection:
    - cloudnumberregistry.projects.locations.customRanges
  async:
    collection:
    - cloudnumberregistry.projects.locations.operations
