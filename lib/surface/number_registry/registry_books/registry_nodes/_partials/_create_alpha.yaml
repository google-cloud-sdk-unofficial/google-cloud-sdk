# -*- coding: utf-8 -*- #
# Copyright 2024 Google LLC. All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#    http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# NOTE: This file is autogenerated and should not be edited by hand.
# AUTOGEN_CLI_VERSION: HEAD
- release_tracks:
  - ALPHA
  help_text:
    brief: Create a registry node resource in a registry book.
    description: |
      Create a registry node resource in a registry book.
    examples: |-
      To create a registry node `my-registry-node` under registry book `my-registry-book` with IP range `10.10.10.0/24` in location `global` run:

      $ {command} my-registry-node --registry-book=my-registry-book --ip-range=10.10.10.0/24 --location=global


      To create a registry node `my-registry-node` under registry book `my-registry-book` with IP range `10.10.20.0/24` with two attributes in location `global` run:

      $ {command} my-registry-node --registry-book=my-registry-book --ip-range=10.10.20.0/24 --attribute=key='gke',value='gke usage' --attribute=key='gce subnet',value='gce subnet for auto gke' --location=global
  arguments:
    params:
    - help_text: |-
        Identifier. name of resource
      is_positional: true
      request_id_field: registryNodeId
      resource_spec: !REF googlecloudsdk.command_lib.number_registry.v1alpha_resources:project_location_registry_book_registry_node
      required: true
    - arg_name: labels
      api_field: registryNode.labels
      required: false
      repeated: true
      help_text: |-
        Labels as key value pairs
      spec:
      - api_field: key
      - api_field: value
    - arg_name: ip-range
      api_field: registryNode.ipRange
      required: true
      repeated: false
      help_text: |-
        IP range of registry node.
    - group:
        required: false
        help_text: |-
          Message describing import metadata.
        params:
        - arg_name: import-info-writer
          api_field: registryNode.importInfo.writer
          required: false
          repeated: false
          help_text: |-
            Writer of imported information.
        - arg_name: import-info-source
          api_field: registryNode.importInfo.source
          required: false
          repeated: false
          help_text: |-
            Resource name of imported source.
    - arg_name: attribute
      api_field: registryNode.attribute
      required: false
      repeated: true
      help_text: |-
        Attributes represent the custom metadata for registry node.
      spec:
      - api_field: key
        help_text: |-
          The key of the registry attribute.
      - api_field: value
        help_text: |-
          The value of the registry node attribute.
      - api_field: description
        help_text: |-
          The description of the registry node attribute.
  request:
    api_version: v1alpha
    collection:
    - cloudnumberregistry.projects.locations.registryBooks.registryNodes
  async:
    collection:
    - cloudnumberregistry.projects.locations.operations
