# -*- coding: utf-8 -*- #
# Copyright 2025 Google LLC. All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#    http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# NOTE: This file is autogenerated and should not be edited by hand.
# AUTOGEN_CLI_VERSION: HEAD
- release_tracks:
  - GA
  auto_generated: true
  help_text:
    brief: Manage the Config of an Addon
    description: Manage the config of an addon
    examples: |-
      To manage the config of an addon with the ID `system-advanced-api-security`, run:

      $ {command} system-advanced-api-security --gateway-plugin-addon-config-configs="pluginInstance=projects/my-project/locations/us-central1/plugins/system-apigee-x-and-hybrid/instances/my-instance-1,apigeeXHybridConfig={environmentFilter={allEnvironments=true}}" --gateway-plugin-addon-config-configs="pluginInstance=projects/my-project/locations/us-central1/plugins/system-edge-public-cloud/instances/my-instance-2,apigeeEdgeConfig={environmentFilter={environments=['env1']}}" --project=my-project --location=us-central1
  arguments:
    params:
    - help_text: |-
        The name of the addon for which the config is to be managed.
        Format: `projects/{project}/locations/{location}/addons/{addon}`.
      is_positional: true
      is_primary_resource: true
      resource_spec: !REF googlecloudsdk.command_lib.apihub.v1_resources:projects_locations_addons
      required: true
    - group:
        mutex: true
        help_text: |-
          Arguments for the config.
        params:
        - group:
            api_field: googleCloudApihubV1ManageAddonConfigRequest.config.gatewayPluginAddonConfig
            required: false
            help_text: |-
              Configuration for gateway plugin addons.
              This is used to specify the list of gateway plugin configs for which the
              addon is enabled.
            params:
            - arg_name: gateway-plugin-addon-config-configs
              api_field: googleCloudApihubV1ManageAddonConfigRequest.config.gatewayPluginAddonConfig.gatewayPluginConfigs
              required: true
              repeated: true
              help_text: |-
                The list of gateway plugin configs for which the addon is enabled.
                Each gateway plugin config should have a unique plugin instance.
              spec:
              - api_field: apigeeXHybridConfig
                one_of_index: 0
                help_text: |-
                  Configuration for Apigee X and Apigee Hybrid gateways.
                spec:
                - api_field: environmentFilter
                  help_text: |-
                    The filter to apply on the resources managed by the gateway plugin
                    instance. If provided this filter applies environment specific filtering.
                  spec:
                  - api_field: allEnvironments
                    help_text: |-
                      Indicates if this filter should match all environments or only
                      a subset of environments.
                      If set to true, all environments are matched.
                  - api_field: environments
                    help_text: |-
                      If provided, only environments in this list are matched. This field is
                      ignored if `all_environments` is true.
              - api_field: apigeeEdgeConfig
                one_of_index: 0
                help_text: |-
                  Configuration for Apigee Edge gateways.
                spec:
                - api_field: environmentFilter
                  help_text: |-
                    The filter to apply on the resources managed by the gateway plugin
                    instance. If provided this filter applies environment specific filtering.
                  spec:
                  - api_field: allEnvironments
                    help_text: |-
                      Indicates if this filter should match all environments or only
                      a subset of environments.
                      If set to true, all environments are matched.
                  - api_field: environments
                    help_text: |-
                      If provided, only environments in this list are matched. This field is
                      ignored if `all_environments` is true.
              - api_field: apigeeOpdkConfig
                one_of_index: 0
                help_text: |-
                  Configuration for Apigee OPDK gateways.
                spec:
                - api_field: environmentFilter
                  help_text: |-
                    The filter to apply on the resources managed by the gateway plugin
                    instance. If provided this filter applies environment specific filtering.
                  spec:
                  - api_field: allEnvironments
                    help_text: |-
                      Indicates if this filter should match all environments or only
                      a subset of environments.
                      If set to true, all environments are matched.
                  - api_field: environments
                    help_text: |-
                      If provided, only environments in this list are matched. This field is
                      ignored if `all_environments` is true.
              - api_field: pluginInstance
                help_text: |-
                  The name of the gateway plugin instance for which the config is to be
                  specified.
                  Format:
                  projects/{project}/locations/{location}/plugins/{plugin}/pluginInstances/{plugin_instance}
        - group:
            api_field: googleCloudApihubV1ManageAddonConfigRequest.config.allDataAddonConfig
            required: false
            help_text: |-
              Configuration for addons which act on all data in the API hub.
              This is used to specify if the addon is enabled for all data in the API hub.
            params:
            - arg_name: all-data-addon-config-enabled
              api_field: googleCloudApihubV1ManageAddonConfigRequest.config.allDataAddonConfig.enabled
              action: store_true_false
              required: true
              type: bool
              help_text: |-
                If true, the addon is enabled for all data in the API hub.
  request:
    api_version: v1
    collection:
    - apihub.projects.locations.addons
    method: manageConfig
  async:
    collection:
    - apihub.projects.locations.operations
  command_type: GENERIC
