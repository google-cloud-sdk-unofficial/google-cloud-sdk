# -*- coding: utf-8 -*- #
# Copyright 2025 Google LLC. All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#    http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# NOTE: This file is autogenerated and should not be edited by hand.
# AUTOGEN_CLI_VERSION: HEAD
- release_tracks:
  - ALPHA
  auto_generated: true
  hidden: true
  help_text:
    brief: Update pluginInstances
    description: Update a pluginInstance
    examples: |-
      To update the pluginInstance, run:

          $ {command}
  arguments:
    params:
    - help_text: |-
        Identifier. The unique name of the plugin instance resource.
        Format:
        `projects/{project}/locations/{location}/plugins/{plugin}/instances/{instance}`
      is_positional: true
      is_primary_resource: true
      resource_spec: !REF googlecloudsdk.command_lib.apihub.v1_resources:projects_locations_plugins_instances
      required: true
    - arg_name: display-name
      api_field: googleCloudApihubV1PluginInstance.displayName
      required: false
      repeated: false
      help_text: |-
        The display name for this plugin instance. Max length is 255 characters.
    - group:
        api_field: googleCloudApihubV1PluginInstance.authConfig
        arg_name: auth-config
        clearable: true
        required: false
        help_text: |-
          AuthConfig represents the authentication information.
        params:
        - group:
            mutex: true
            help_text: |-
              Arguments for the config.
            params:
            - group:
                api_field: googleCloudApihubV1PluginInstance.authConfig.userPasswordConfig
                required: false
                help_text: |-
                  Parameters to support Username and Password Authentication.
                params:
                - arg_name: user-password-config-username
                  api_field: googleCloudApihubV1PluginInstance.authConfig.userPasswordConfig.username
                  required: false
                  repeated: false
                  help_text: |-
                    Username.
                - group:
                    api_field: googleCloudApihubV1PluginInstance.authConfig.userPasswordConfig.password
                    required: false
                    help_text: |-
                      Secret provides a reference to entries in Secret Manager.
                    params:
                    - arg_name: user-password-config-secret-version
                      api_field: googleCloudApihubV1PluginInstance.authConfig.userPasswordConfig.password.secretVersion
                      required: false
                      repeated: false
                      help_text: |-
                        The resource name of the secret version in the format,
                        format as: `projects/*/secrets/*/versions/*`.
            - group:
                api_field: googleCloudApihubV1PluginInstance.authConfig.apiKeyConfig
                required: false
                help_text: |-
                  Config for authentication with API key.
                params:
                - arg_name: api-key-config-name
                  api_field: googleCloudApihubV1PluginInstance.authConfig.apiKeyConfig.name
                  required: false
                  repeated: false
                  help_text: |-
                    The parameter name of the API key.
                    E.g. If the API request is "https://example.com/act?api_key=<API KEY>",
                    "api_key" would be the parameter name.
                - group:
                    api_field: googleCloudApihubV1PluginInstance.authConfig.apiKeyConfig.apiKey
                    required: false
                    help_text: |-
                      Secret provides a reference to entries in Secret Manager.
                    params:
                    - arg_name: api-key-config-secret-version
                      api_field: googleCloudApihubV1PluginInstance.authConfig.apiKeyConfig.apiKey.secretVersion
                      required: false
                      repeated: false
                      help_text: |-
                        The resource name of the secret version in the format,
                        format as: `projects/*/secrets/*/versions/*`.
                - arg_name: api-key-config-http-element-location
                  api_field: googleCloudApihubV1PluginInstance.authConfig.apiKeyConfig.httpElementLocation
                  required: false
                  help_text: |-
                    The location of the API key.
                    The default value is QUERY.
                  choices:
                  - arg_value: query
                    enum_value: QUERY
                    help_text: |-
                      Element is in the HTTP request query.
                  - arg_value: header
                    enum_value: HEADER
                    help_text: |-
                      Element is in the HTTP request header.
                  - arg_value: path
                    enum_value: PATH
                    help_text: |-
                      Element is in the HTTP request path.
                  - arg_value: body
                    enum_value: BODY
                    help_text: |-
                      Element is in the HTTP request body.
                  - arg_value: cookie
                    enum_value: COOKIE
                    help_text: |-
                      Element is in the HTTP request cookie.
            - group:
                api_field: googleCloudApihubV1PluginInstance.authConfig.oauth2ClientCredentialsConfig
                required: false
                help_text: |-
                  Parameters to support Oauth 2.0 client credentials grant authentication.
                  See https://tools.ietf.org/html/rfc6749#section-1.3.4 for more details.
                params:
                - arg_name: oauth2-client-credentials-config-id
                  api_field: googleCloudApihubV1PluginInstance.authConfig.oauth2ClientCredentialsConfig.clientId
                  required: false
                  repeated: false
                  help_text: |-
                    The client identifier.
                - group:
                    api_field: googleCloudApihubV1PluginInstance.authConfig.oauth2ClientCredentialsConfig.clientSecret
                    required: false
                    help_text: |-
                      Secret provides a reference to entries in Secret Manager.
                    params:
                    - arg_name: oauth2-client-credentials-config-secret-version
                      api_field: googleCloudApihubV1PluginInstance.authConfig.oauth2ClientCredentialsConfig.clientSecret.secretVersion
                      required: false
                      repeated: false
                      help_text: |-
                        The resource name of the secret version in the format,
                        format as: `projects/*/secrets/*/versions/*`.
        - arg_name: auth-config-type
          api_field: googleCloudApihubV1PluginInstance.authConfig.authType
          required: false
          help_text: |-
            The authentication type.
          choices:
          - arg_value: no-auth
            enum_value: NO_AUTH
            help_text: |-
              No authentication.
          - arg_value: google-service-account
            enum_value: GOOGLE_SERVICE_ACCOUNT
            help_text: |-
              Google service account authentication.
          - arg_value: user-password
            enum_value: USER_PASSWORD
            help_text: |-
              Username and password authentication.
          - arg_value: api-key
            enum_value: API_KEY
            help_text: |-
              API Key authentication.
          - arg_value: oauth2-client-credentials
            enum_value: OAUTH2_CLIENT_CREDENTIALS
            help_text: |-
              Oauth 2.0 client credentials grant authentication.
    - arg_name: additional-config
      api_field: googleCloudApihubV1PluginInstance.additionalConfig
      required: false
      repeated: true
      help_text: |-
        The additional information for this plugin instance corresponding to the
        additional config template of the plugin. This information will be sent to
        plugin hosting service on each call to plugin hosted service.
        The key will be the config_variable_template.display_name to uniquely
        identify the config variable.
      clearable: true
      spec:
      - api_field: key
      - api_field: value
        spec:
        - api_field: stringValue
          one_of_index: 0
          help_text: |-
            The config variable value in case of config variable of type string.
        - api_field: intValue
          one_of_index: 0
          help_text: |-
            The config variable value in case of config variable of type integer.
        - api_field: boolValue
          one_of_index: 0
          help_text: |-
            The config variable value in case of config variable of type boolean.
        - api_field: secretValue
          one_of_index: 0
          help_text: |-
            The config variable value in case of config variable of type secret.
          spec:
          - api_field: secretVersion
            help_text: |-
              The resource name of the secret version in the format,
              format as: `projects/*/secrets/*/versions/*`.
        - api_field: enumValue
          one_of_index: 0
          help_text: |-
            The config variable value in case of config variable of type enum.
          spec:
          - api_field: id
            help_text: |-
              Id of the option.
          - api_field: displayName
            help_text: |-
              Display name of the option.
          - api_field: description
            help_text: |-
              Description of the option.
        - api_field: multiSelectValues
          one_of_index: 0
          help_text: |-
            The config variable value in case of config variable of type multi
            select.
          spec:
          - api_field: values
            help_text: |-
              The config variable value of data type multi select.
            spec:
            - api_field: id
              help_text: |-
                Id of the option.
            - api_field: displayName
              help_text: |-
                Display name of the option.
            - api_field: description
              help_text: |-
                Description of the option.
        - api_field: multiStringValues
          one_of_index: 0
          help_text: |-
            The config variable value in case of config variable of type multi
            string.
          spec:
          - api_field: values
            help_text: |-
              The config variable value of data type multi string.
        - api_field: multiIntValues
          one_of_index: 0
          help_text: |-
            The config variable value in case of config variable of type multi
            integer.
          spec:
          - api_field: values
            help_text: |-
              The config variable value of data type multi int.
    - arg_name: actions
      api_field: googleCloudApihubV1PluginInstance.actions
      required: false
      repeated: true
      help_text: |-
        The action status for the plugin instance.
      clearable: true
      spec:
      - api_field: actionId
        help_text: |-
          This should map to one of the [action id][google.cloud.apihub.v1.PluginActionConfig.id] specified
          in [actions_config][google.cloud.apihub.v1.Plugin.actions_config] in the plugin.
      - api_field: scheduleCronExpression
        help_text: |-
          The schedule for this plugin instance action. This can only be set if the
          plugin supports API_HUB_SCHEDULE_TRIGGER mode for this action.
      - api_field: curationConfig
        help_text: |-
          This configuration should be provided if the plugin action is publishing
          data to API hub curate layer.
        spec:
        - api_field: customCuration
          one_of_index: 0
          help_text: |-
            Custom curation information for this plugin instance.
          spec:
          - api_field: curation
            help_text: |-
              The unique name of the curation resource. This will be the name of the
              curation resource in the format:
              `projects/{project}/locations/{location}/curations/{curation}`
        - api_field: curationType
          help_text: |-
            The curation type for this plugin instance.
      - api_field: scheduleTimeZone
        help_text: |-
          The time zone for the schedule cron expression. If not provided, UTC will
          be used.
      - api_field: serviceAccount
        help_text: |-
          The service account used to publish data. Note, the service account will
          only be accepted for non-Google Cloud plugins like OPDK.
    - arg_name: source-project-id
      api_field: googleCloudApihubV1PluginInstance.sourceProjectId
      required: false
      repeated: false
      help_text: |-
        The source project id of the plugin instance. This will be the id of
        runtime project in case of Google Cloud based plugins and org id in case of
        non-Google Cloud based plugins. This field will be a required field for
        Google provided on-ramp plugins.
    - arg_name: source-environments-config
      api_field: googleCloudApihubV1PluginInstance.sourceEnvironmentsConfig
      required: false
      repeated: true
      help_text: |-
        The source environment's config present in the gateway instance linked to
        the plugin instance.
        The key is the `source_environment` name from the SourceEnvironment
        message.
      clearable: true
      spec:
      - api_field: key
      - api_field: value
        spec:
        - api_field: sourceEnvironment
          help_text: |-
            The name of the environment at the source. This should map to
            [Deployment][google.cloud.apihub.v1.SourceEnvironment.source_environment].
        - api_field: sourceEnvironmentUri
          help_text: |-
            The location where additional information about source environments can be
            found. The location should be relative path of the environment manifest
            with respect to a plugin instance.
        - api_field: createTime
          help_text: |-
            The time at which the environment was created at the source.
          spec:
          - api_field: seconds
            help_text: |-
              Represents seconds of UTC time since Unix epoch 1970-01-01T00:00:00Z. Must
              be between -62135596800 and 253402300799 inclusive (which corresponds to
              0001-01-01T00:00:00Z to 9999-12-31T23:59:59Z).
          - api_field: nanos
            help_text: |-
              Non-negative fractions of a second at nanosecond resolution. This field is
              the nanosecond portion of the duration, not an alternative to seconds.
              Negative second values with fractions must still have non-negative nanos
              values that count forward in time. Must be between 0 and 999,999,999
              inclusive.
        - api_field: updateTime
          help_text: |-
            The time at which the environment was last updated at the source.
          spec:
          - api_field: seconds
            help_text: |-
              Represents seconds of UTC time since Unix epoch 1970-01-01T00:00:00Z. Must
              be between -62135596800 and 253402300799 inclusive (which corresponds to
              0001-01-01T00:00:00Z to 9999-12-31T23:59:59Z).
          - api_field: nanos
            help_text: |-
              Non-negative fractions of a second at nanosecond resolution. This field is
              the nanosecond portion of the duration, not an alternative to seconds.
              Negative second values with fractions must still have non-negative nanos
              values that count forward in time. Must be between 0 and 999,999,999
              inclusive.
  request:
    api_version: v1
    collection:
    - apihub.projects.locations.plugins.instances
  update:
    read_modify_update: true
