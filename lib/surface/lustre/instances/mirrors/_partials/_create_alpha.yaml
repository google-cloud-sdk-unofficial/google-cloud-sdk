# -*- coding: utf-8 -*- #
# Copyright 2026 Google LLC. All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#    http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# NOTE: This file is autogenerated and should not be edited by hand.
# AUTOGEN_CLI_VERSION: HEAD
- release_tracks:
  - ALPHA
  auto_generated: true
  help_text:
    brief: Creates a Managed Lustre mirror
    description: |
      Creates a Managed Lustre mirror for an instance.
    examples: |-
      To create a mirror `my-mirror` for instance `my-instance` in location `us-central1-a` syncing from `gs://my-bucket/data/` to `/data` run:

      $ {command} my-mirror --instance=my-instance --location=us-central1-a --gcs-path-uri=gs://my-bucket/data/ --lustre-path=/data --direction=from-cloud-storage
  arguments:
    params:
    - help_text: |-
        Identifier. Name of the mirror.
        Format:
        projects/{project}/locations/{location}/instances/{instance}/mirrors/{mirror}
      is_positional: true
      is_primary_resource: true
      request_id_field: mirrorId
      resource_spec: !REF googlecloudsdk.command_lib.lustre.v1alpha_resources:projects_locations_instances_mirrors
      required: true
    - group:
        api_field: mirror.gcsPath
        required: true
        help_text: |-
          Specifies a Cloud Storage bucket and, optionally, a path inside the bucket.
        params:
        - arg_name: gcs-path-uri
          api_field: mirror.gcsPath.uri
          required: true
          repeated: false
          help_text: |-
            The URI to a Cloud Storage bucket, or a path within a bucket, using the
            format `gs://<bucket_name>/<optional_path_inside_bucket>/`. If a path
            inside the bucket is specified, it must end with a forward slash (`/`).
    - group:
        api_field: mirror.lustrePath
        required: true
        help_text: |-
          The root directory path to the Lustre file system.
        params:
        - arg_name: lustre-path
          api_field: mirror.lustrePath.path
          required: false
          repeated: false
          help_text: |-
            The root directory path to the Managed Lustre file system. Must start with
            `/`. Default is `/`. If you're importing data into Managed Lustre, any
            path other than the default must already exist on the file system.
    - arg_name: direction
      api_field: mirror.direction
      required: true
      help_text: |-
        Represents the direction of the mirror.
      choices:
      - arg_value: from-cloud-storage
        enum_value: FROM_CLOUD_STORAGE
        help_text: |-
          Mirror from Cloud Storage to Lustre.
    - arg_name: deleted-files-retained
      api_field: mirror.deletedFilesRetained
      action: store_true
      required: false
      type: bool
      help_text: |-
        If true, files will be retained in lustre after the corresponding files are
        deleted from cloud storage.
        Default is false.
      default: null
    - arg_name: description
      api_field: mirror.description
      required: false
      repeated: false
      help_text: |-
        Description of the mirror.
    - arg_name: labels
      api_field: mirror.labels
      required: false
      repeated: true
      help_text: |-
        Labels to apply to the mirror.
      spec:
      - api_field: key
      - api_field: value
    - arg_name: request-id
      api_field: requestId
      required: false
      repeated: false
      help_text: |-
        The unique ID to identify requests. Specify a unique request ID
        so that if you must retry your request, the server will know to ignore
        the request if it has already been completed. The server guarantees that a
        request doesn't result in creation of duplicate mirrors for at least 60
        minutes.

        For example, consider a situation where you make an initial request and the
        request times out. If you make the request again with the same request
        ID, the server can check if original operation with the same request ID
        was received, and if so, will ignore the second request. This prevents
        clients from accidentally creating duplicate mirrors.

        The request ID must be a valid UUID version 4 with the exception that zero
        UUID is not supported (`00000000-0000-0000-0000-000000000000`).
        This request is only idempotent if a `request_id` is provided.
  request:
    api_version: v1alpha
    collection:
    - lustre.projects.locations.instances.mirrors
  async:
    collection:
    - lustre.projects.locations.operations
