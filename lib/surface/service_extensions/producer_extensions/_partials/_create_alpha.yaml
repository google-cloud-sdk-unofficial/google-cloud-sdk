# -*- coding: utf-8 -*- #
# Copyright 2026 Google LLC. All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#    http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# NOTE: This file is autogenerated and should not be edited by hand.
# AUTOGEN_CLI_VERSION: HEAD
- release_tracks:
  - ALPHA
  auto_generated: true
  help_text:
    brief: Create producerExtensions
    description: Create a producerExtension
    examples: |-
      To create the producerExtension, run:

          $ {command}
  arguments:
    params:
    - help_text: |-
        Identifier. Name of the `ProducerExtension` resource in the following format:
        `projects/{project}/locations/{location}/producerExtensions/{producer_extension}`.
      is_positional: true
      is_primary_resource: true
      request_id_field: producerExtensionId
      resource_spec: !REF googlecloudsdk.command_lib.service_extensions.v1alpha1_resources:projects_locations_producer_extensions
      required: true
    - arg_name: description
      api_field: producerExtension.description
      required: false
      repeated: false
      help_text: |-
        A human-readable description of the resource.
    - arg_name: labels
      api_field: producerExtension.labels
      required: false
      repeated: true
      help_text: |-
        Set of labels associated with the `ProducerExtension` resource.

        The format must comply with [the following
        requirements](https://cloud.google.com/compute/docs/labeling-resources#requirements).
      spec:
      - api_field: key
      - api_field: value
    - arg_name: etag
      api_field: producerExtension.etag
      required: false
      repeated: false
      help_text: |-
        Etag of the resource.
        If this is provided, it must match the server's etag. If the provided etag
        does not match the server's etag, the request will fail with a 409 ABORTED
        error.
    - arg_name: phase
      api_field: producerExtension.phase
      required: true
      help_text: |-
        The phase in which this `ProducerExtension` should execute.
      choices:
      - arg_value: traffic
        enum_value: TRAFFIC
        help_text: |-
          The `ProducerExtension` will be executed during the traffic phase.
      - arg_value: authz
        enum_value: AUTHZ
        help_text: |-
          The `ProducerExtension` will be executed during the authorization phase.
    - group:
        api_field: producerExtension.extensionSettings
        required: true
        help_text: |-
          The configuration for the service that this `ProducerExtension` offers.
        params:
        - arg_name: extension-settings-service
          api_field: producerExtension.extensionSettings.service
          required: true
          repeated: false
          help_text: |-
            URI of the PSC attachment.
        - arg_name: extension-settings-authority
          api_field: producerExtension.extensionSettings.authority
          required: false
          repeated: false
          help_text: |-
            The `:authority` header in the request sent to the extension service.
        - arg_name: extension-settings-observability-mode
          api_field: producerExtension.extensionSettings.observabilityMode
          action: store_true
          required: false
          type: bool
          help_text: |-
            Whether the extension should function in observability mode.
          default: null
        - arg_name: extension-settings-supported-events
          api_field: producerExtension.extensionSettings.supportedEvents
          required: true
          repeated: true
          help_text: |-
            The event types supported by the extension.
          choices:
          - arg_value: request-headers
            enum_value: REQUEST_HEADERS
            help_text: |-
              If included in `supported_events`,
              the extension is called when the HTTP request headers arrive.
          - arg_value: request-body
            enum_value: REQUEST_BODY
            help_text: |-
              If included in `supported_events`,
              the extension is called when the HTTP request body arrives.
          - arg_value: response-headers
            enum_value: RESPONSE_HEADERS
            help_text: |-
              If included in `supported_events`,
              the extension is called when the HTTP response headers arrive.
          - arg_value: response-body
            enum_value: RESPONSE_BODY
            help_text: |-
              If included in `supported_events`,
              the extension is called when the HTTP response body arrives.
          - arg_value: request-trailers
            enum_value: REQUEST_TRAILERS
            help_text: |-
              If included in `supported_events`,
              the extension is called when the HTTP request trailers arrives.
          - arg_value: response-trailers
            enum_value: RESPONSE_TRAILERS
            help_text: |-
              If included in `supported_events`,
              the extension is called when the HTTP response trailers arrives.
  request:
    api_version: v1alpha1
    collection:
    - networkservices.projects.locations.producerExtensions
  async:
    collection:
    - networkservices.projects.locations.operations
