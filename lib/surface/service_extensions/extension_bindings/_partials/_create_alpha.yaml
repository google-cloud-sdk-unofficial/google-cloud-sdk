# -*- coding: utf-8 -*- #
# Copyright 2026 Google LLC. All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#    http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# NOTE: This file is autogenerated and should not be edited by hand.
# AUTOGEN_CLI_VERSION: HEAD
- release_tracks:
  - ALPHA
  auto_generated: true
  help_text:
    brief: Create extensionBindings
    description: Create an extensionBinding
    examples: |-
      To create the extensionBinding, run:

          $ {command}
  arguments:
    params:
    - help_text: |-
        Identifier. Name of the `ExtensionBinding` resource in the following format:
        `projects/{project}/locations/{location}/extensionBindings/{extension_binding}`.
      is_positional: true
      is_primary_resource: true
      request_id_field: extensionBindingId
      resource_spec: !REF googlecloudsdk.command_lib.service_extensions.v1alpha1_resources:projects_locations_extension_bindings
      required: true
    - arg_name: description
      api_field: extensionBinding.description
      required: false
      repeated: false
      help_text: |-
        A human-readable description of the resource.
    - arg_name: labels
      api_field: extensionBinding.labels
      required: false
      repeated: true
      help_text: |-
        Set of labels associated with the `ExtensionBinding` resource.

        The format must comply with [the following
        requirements](https://cloud.google.com/compute/docs/labeling-resources#requirements).
      spec:
      - api_field: key
      - api_field: value
    - arg_name: etag
      api_field: extensionBinding.etag
      required: false
      repeated: false
      help_text: |-
        Etag of the resource.
        If provided, it must match the server's etag. If the provided etag
        does not match the server's etag, the request will fail with a 409 ABORTED
        error.
    - arg_name: producer-extension
      api_field: extensionBinding.producerExtension
      required: true
      repeated: false
      help_text: |-
        The name of the extension that this binding should attach to target
        resources.

        Format:
        For Google-provided extensions, specify the service endpoint (see
        [Model Armor
        integration](https://docs.cloud.google.com/model-armor/integrations))
    - group:
        api_field: extensionBinding.target
        required: true
        help_text: |-
          Specifies a list of targets to which this `ExtensionBinding` should attach.
        params:
        - arg_name: target-resources
          api_field: extensionBinding.target.resources
          required: false
          repeated: true
          help_text: |-
            The reference to the target resource, to which this binding should
            attach. Exactly one of `resources` or `scope` must be set.
        - group:
            api_field: extensionBinding.target.scope
            required: false
            help_text: |-
              Specifies the scope of resources to which this binding should attach.
            params:
            - arg_name: scope-parent
              api_field: extensionBinding.target.scope.parent
              required: true
              repeated: false
              help_text: |-
                Parent resource name specification, in the format:
                `projects/{project_number}`.
            - arg_name: scope-resource-types
              api_field: extensionBinding.target.scope.resourceTypes
              required: true
              repeated: true
              help_text: |-
                Type of the resource to which the binding should attach.
                Limited to 1 resource type.
              choices:
              - arg_value: ai-application
                enum_value: AI_APPLICATION
                help_text: |-
                  AI Application resources.
              - arg_value: agent-gateway
                enum_value: AGENT_GATEWAY
                help_text: |-
                  Agent Gateway resources.
    - arg_name: match-conditions
      api_field: extensionBinding.matchConditions
      required: false
      repeated: true
      help_text: |-
        A list of match conditions to match against the incoming
        request. The extension will be invoked if at least one condition matches
        the request, or if no match conditions are specified. Limited to 5
        conditions.
      spec:
      - api_field: to
        help_text: |-
          Describes properties of a destination of a request. If specified, the
          extension will only be invoked on requests to destinations that match the
          specified criteria.
        spec:
        - api_field: destination
          help_text: |-
            Describes properties of destination of a request. Within a destination,
            the match follows AND semantics across fields and OR semantics within a
            field, i.e. a match occurs when ANY path matches AND ANY header matches
            and ANY method matches. At least one of destination or not_destination
            must be specified.
          spec:
          - api_field: headerSet
            help_text: |-
              A set of HTTP headers to match against. If not specified, requests
              with any headers are matched.
            spec:
            - api_field: headers
              help_text: |-
                A list of headers to match against in http header.
                If multiple header matches are provided, they will be evaluated as
                an AND, i.e. all header matches must match for the request to
                match.
              spec:
              - api_field: name
                help_text: |-
                  Specifies the name of the header in the request.
              - api_field: value
                help_text: |-
                  Specifies how the header match will be performed.
                spec:
                - api_field: exact
                  one_of_index: 0
                  help_text: |-
                    The input string must match exactly the string specified here.

                    Examples:

                    * ``abc`` only matches the value ``abc``.
                - api_field: prefix
                  one_of_index: 0
                  help_text: |-
                    The input string must have the prefix specified here.
                    Note: empty prefix is not allowed.

                    Examples:

                    * ``abc`` matches the value ``abc.xyz``
                - api_field: suffix
                  one_of_index: 0
                  help_text: |-
                    The input string must have the suffix specified here.
                    Note: empty prefix is not allowed, please use regex instead.

                    Examples:

                    * ``abc`` matches the value ``xyz.abc``
                - api_field: contains
                  one_of_index: 0
                  help_text: |-
                    The input string must have the substring specified here.
                    Note: empty contains match is not allowed, please use regex instead.

                    Examples:

                    * ``abc`` matches the value ``xyz.abc.def``
                - api_field: ignoreCase
                  help_text: |-
                    If true, indicates the exact/prefix/suffix/contains matching should be
                    case insensitive. For example, the matcher ``data`` will match both
                    input string ``Data`` and ``data`` if set to true.
          - api_field: resources
            help_text: |-
              A list of non-empty strings whose value is matched against the
              resource value. If not specified, any resource is allowed.
              If specified, a match occurs if any of the resources
              matches the resource value in the request. Limited to 5 resources.
            spec:
            - api_field: exact
              one_of_index: 0
              help_text: |-
                The input string must match exactly the string specified here.

                Examples:

                * ``abc`` only matches the value ``abc``.
            - api_field: prefix
              one_of_index: 0
              help_text: |-
                The input string must have the prefix specified here.
                Note: empty prefix is not allowed.

                Examples:

                * ``abc`` matches the value ``abc.xyz``
            - api_field: suffix
              one_of_index: 0
              help_text: |-
                The input string must have the suffix specified here.
                Note: empty prefix is not allowed, please use regex instead.

                Examples:

                * ``abc`` matches the value ``xyz.abc``
            - api_field: contains
              one_of_index: 0
              help_text: |-
                The input string must have the substring specified here.
                Note: empty contains match is not allowed, please use regex instead.

                Examples:

                * ``abc`` matches the value ``xyz.abc.def``
            - api_field: ignoreCase
              help_text: |-
                If true, indicates the exact/prefix/suffix/contains matching should be
                case insensitive. For example, the matcher ``data`` will match both
                input string ``Data`` and ``data`` if set to true.
          - api_field: hosts
            help_text: |-
              A list of HTTP Hosts to match against. Limited to 10 hosts. If not
              specified, any host is allowed.
              If specified, a match occurs if any of the hosts matches the host
              value in the request.
            spec:
            - api_field: exact
              one_of_index: 0
              help_text: |-
                The input string must match exactly the string specified here.

                Examples:

                * ``abc`` only matches the value ``abc``.
            - api_field: prefix
              one_of_index: 0
              help_text: |-
                The input string must have the prefix specified here.
                Note: empty prefix is not allowed.

                Examples:

                * ``abc`` matches the value ``abc.xyz``
            - api_field: suffix
              one_of_index: 0
              help_text: |-
                The input string must have the suffix specified here.
                Note: empty prefix is not allowed, please use regex instead.

                Examples:

                * ``abc`` matches the value ``xyz.abc``
            - api_field: contains
              one_of_index: 0
              help_text: |-
                The input string must have the substring specified here.
                Note: empty contains match is not allowed, please use regex instead.

                Examples:

                * ``abc`` matches the value ``xyz.abc.def``
            - api_field: ignoreCase
              help_text: |-
                If true, indicates the exact/prefix/suffix/contains matching should be
                case insensitive. For example, the matcher ``data`` will match both
                input string ``Data`` and ``data`` if set to true.
          - api_field: paths
            help_text: |-
              A list of paths to match against. Limited to 10
              paths. If not specified, any path is allowed.

              Note that this path match includes the query parameters. For gRPC
              services, this should be a fully-qualified name of the form
              /package.service/method.
            spec:
            - api_field: exact
              one_of_index: 0
              help_text: |-
                The input string must match exactly the string specified here.

                Examples:

                * ``abc`` only matches the value ``abc``.
            - api_field: prefix
              one_of_index: 0
              help_text: |-
                The input string must have the prefix specified here.
                Note: empty prefix is not allowed.

                Examples:

                * ``abc`` matches the value ``abc.xyz``
            - api_field: suffix
              one_of_index: 0
              help_text: |-
                The input string must have the suffix specified here.
                Note: empty prefix is not allowed, please use regex instead.

                Examples:

                * ``abc`` matches the value ``xyz.abc``
            - api_field: contains
              one_of_index: 0
              help_text: |-
                The input string must have the substring specified here.
                Note: empty contains match is not allowed, please use regex instead.

                Examples:

                * ``abc`` matches the value ``xyz.abc.def``
            - api_field: ignoreCase
              help_text: |-
                If true, indicates the exact/prefix/suffix/contains matching should be
                case insensitive. For example, the matcher ``data`` will match both
                input string ``Data`` and ``data`` if set to true.
        - api_field: notDestination
          help_text: |-
            Describes the negated properties of the request destination. Extension
            will not be invoked on requests that match the criteria specified in
            this field. At least one of destination or not_destination must be
            specified.
          spec:
          - api_field: headerSet
            help_text: |-
              A set of HTTP headers to match against. If not specified, requests
              with any headers are matched.
            spec:
            - api_field: headers
              help_text: |-
                A list of headers to match against in http header.
                If multiple header matches are provided, they will be evaluated as
                an AND, i.e. all header matches must match for the request to
                match.
              spec:
              - api_field: name
                help_text: |-
                  Specifies the name of the header in the request.
              - api_field: value
                help_text: |-
                  Specifies how the header match will be performed.
                spec:
                - api_field: exact
                  one_of_index: 0
                  help_text: |-
                    The input string must match exactly the string specified here.

                    Examples:

                    * ``abc`` only matches the value ``abc``.
                - api_field: prefix
                  one_of_index: 0
                  help_text: |-
                    The input string must have the prefix specified here.
                    Note: empty prefix is not allowed.

                    Examples:

                    * ``abc`` matches the value ``abc.xyz``
                - api_field: suffix
                  one_of_index: 0
                  help_text: |-
                    The input string must have the suffix specified here.
                    Note: empty prefix is not allowed, please use regex instead.

                    Examples:

                    * ``abc`` matches the value ``xyz.abc``
                - api_field: contains
                  one_of_index: 0
                  help_text: |-
                    The input string must have the substring specified here.
                    Note: empty contains match is not allowed, please use regex instead.

                    Examples:

                    * ``abc`` matches the value ``xyz.abc.def``
                - api_field: ignoreCase
                  help_text: |-
                    If true, indicates the exact/prefix/suffix/contains matching should be
                    case insensitive. For example, the matcher ``data`` will match both
                    input string ``Data`` and ``data`` if set to true.
          - api_field: resources
            help_text: |-
              A list of non-empty strings whose value is matched against the
              resource value. If not specified, any resource is allowed.
              If specified, a match occurs if any of the resources
              matches the resource value in the request. Limited to 5 resources.
            spec:
            - api_field: exact
              one_of_index: 0
              help_text: |-
                The input string must match exactly the string specified here.

                Examples:

                * ``abc`` only matches the value ``abc``.
            - api_field: prefix
              one_of_index: 0
              help_text: |-
                The input string must have the prefix specified here.
                Note: empty prefix is not allowed.

                Examples:

                * ``abc`` matches the value ``abc.xyz``
            - api_field: suffix
              one_of_index: 0
              help_text: |-
                The input string must have the suffix specified here.
                Note: empty prefix is not allowed, please use regex instead.

                Examples:

                * ``abc`` matches the value ``xyz.abc``
            - api_field: contains
              one_of_index: 0
              help_text: |-
                The input string must have the substring specified here.
                Note: empty contains match is not allowed, please use regex instead.

                Examples:

                * ``abc`` matches the value ``xyz.abc.def``
            - api_field: ignoreCase
              help_text: |-
                If true, indicates the exact/prefix/suffix/contains matching should be
                case insensitive. For example, the matcher ``data`` will match both
                input string ``Data`` and ``data`` if set to true.
          - api_field: hosts
            help_text: |-
              A list of HTTP Hosts to match against. Limited to 10 hosts. If not
              specified, any host is allowed.
              If specified, a match occurs if any of the hosts matches the host
              value in the request.
            spec:
            - api_field: exact
              one_of_index: 0
              help_text: |-
                The input string must match exactly the string specified here.

                Examples:

                * ``abc`` only matches the value ``abc``.
            - api_field: prefix
              one_of_index: 0
              help_text: |-
                The input string must have the prefix specified here.
                Note: empty prefix is not allowed.

                Examples:

                * ``abc`` matches the value ``abc.xyz``
            - api_field: suffix
              one_of_index: 0
              help_text: |-
                The input string must have the suffix specified here.
                Note: empty prefix is not allowed, please use regex instead.

                Examples:

                * ``abc`` matches the value ``xyz.abc``
            - api_field: contains
              one_of_index: 0
              help_text: |-
                The input string must have the substring specified here.
                Note: empty contains match is not allowed, please use regex instead.

                Examples:

                * ``abc`` matches the value ``xyz.abc.def``
            - api_field: ignoreCase
              help_text: |-
                If true, indicates the exact/prefix/suffix/contains matching should be
                case insensitive. For example, the matcher ``data`` will match both
                input string ``Data`` and ``data`` if set to true.
          - api_field: paths
            help_text: |-
              A list of paths to match against. Limited to 10
              paths. If not specified, any path is allowed.

              Note that this path match includes the query parameters. For gRPC
              services, this should be a fully-qualified name of the form
              /package.service/method.
            spec:
            - api_field: exact
              one_of_index: 0
              help_text: |-
                The input string must match exactly the string specified here.

                Examples:

                * ``abc`` only matches the value ``abc``.
            - api_field: prefix
              one_of_index: 0
              help_text: |-
                The input string must have the prefix specified here.
                Note: empty prefix is not allowed.

                Examples:

                * ``abc`` matches the value ``abc.xyz``
            - api_field: suffix
              one_of_index: 0
              help_text: |-
                The input string must have the suffix specified here.
                Note: empty prefix is not allowed, please use regex instead.

                Examples:

                * ``abc`` matches the value ``xyz.abc``
            - api_field: contains
              one_of_index: 0
              help_text: |-
                The input string must have the substring specified here.
                Note: empty contains match is not allowed, please use regex instead.

                Examples:

                * ``abc`` matches the value ``xyz.abc.def``
            - api_field: ignoreCase
              help_text: |-
                If true, indicates the exact/prefix/suffix/contains matching should be
                case insensitive. For example, the matcher ``data`` will match both
                input string ``Data`` and ``data`` if set to true.
    - arg_name: fail-open
      api_field: extensionBinding.failOpen
      action: store_true
      required: false
      type: bool
      help_text: |-
        Determines the behavior of the extension binding when the call to the
        extension fails or times out. Default value is `FALSE`.

        When set to `TRUE`, failures of the extension are silently ignored.
      default: null
    - arg_name: producer-metadata
      api_field: extensionBinding.producerMetadata
      required: false
      repeated: true
      help_text: |-
        Additional metadata that should be passed to the attached extension
        with each request.
      spec:
      - api_field: key
      - api_field: value
    - arg_name: priority
      api_field: extensionBinding.priority
      required: false
      repeated: false
      type: int
      help_text: |-
        Priority of the extension binding. Lower numbers indicate higher priority.
        Priority of extension bindings are used to determine the order in which
        extension bindings are applied to a request.
  request:
    api_version: v1alpha1
    collection:
    - networkservices.projects.locations.extensionBindings
  async:
    collection:
    - networkservices.projects.locations.operations
