- release_tracks: [ALPHA]

  help_text:
    brief: describe a template
    description: describes a template
    examples: |
      To describe a template `test-template', run:

      $ {command} test-template

  request:
    collection: telecomdatafabric.projects.locations.templates

  arguments:
    resource:
      help_text: TelecomDataFabric template
      spec: !REF googlecloudsdk.command_lib.telecomdatafabric.resources:template
