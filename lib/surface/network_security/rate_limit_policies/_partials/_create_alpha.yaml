# -*- coding: utf-8 -*- #
# Copyright 2026 Google LLC. All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#    http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# NOTE: This file is autogenerated and should not be edited by hand.
# AUTOGEN_CLI_VERSION: HEAD
- release_tracks:
  - ALPHA
  auto_generated: true
  hidden: true
  help_text:
    brief: Create a RateLimitPolicy resource
    description: Create a RateLimitPolicy resource.
    examples: |-
      To create `RateLimitPolicy` resource `my-rate-limit-policy` in location `global`, run:

      $ {command} --location=global my-rate-limit-policy
  arguments:
    params:
    - help_text: |-
        Identifier. Specifies the name of the `RateLimitPolicy` resource.
      is_positional: true
      is_primary_resource: true
      request_id_field: rateLimitPolicyId
      resource_spec: !REF googlecloudsdk.command_lib.network_security.v1alpha1_resources:projects_locations_rate_limit_policies
      required: true
    - arg_name: labels
      api_field: rateLimitPolicy.labels
      required: false
      repeated: true
      help_text: |-
        Stores labels as key value pairs.
      spec:
      - api_field: key
      - api_field: value
    - arg_name: description
      api_field: rateLimitPolicy.description
      required: false
      repeated: false
      help_text: |-
        Provides a human-readable description of the resource.
    - arg_name: targets
      api_field: rateLimitPolicy.targets
      required: true
      repeated: true
      help_text: |-
        Specifies a list of targets to which this policy applies.
      spec:
      - api_field: gatewayResource
        help_text: |-
          Reference to a Gateway resource on which this policy will
          be applied.
    - arg_name: http-rules
      api_field: rateLimitPolicy.httpRules
      required: false
      repeated: true
      help_text: |-
        Specifies a list of rate limit HTTP rules to match against the incoming
        request.
        Limited to 5 HTTP rules per RateLimitPolicy.
      spec:
      - api_field: to
        help_text: |-
          Describes properties of a target of a request.
        spec:
        - api_field: destination
          help_text: |-
            Describes properties of a request's destination. At least
            one of destination or not_destination must be specified. A match
            occurs when ANY fields in either destination or not_destination matches
            the request. Within a destination, the match follows OR semantics
            across fields and AND semantics within a single field.
          spec:
          - api_field: headerSet
            help_text: |-
              Specifies a list of headers to match against in http header.
            spec:
            - api_field: headers
              help_text: |-
                Contains a list of headers to match against in http header.
                The match can be one of exact, prefix, suffix, or contains
                (substring match). The match follows AND semantics which means all
                the headers must match. Matches are always case sensitive unless
                the ignoreCase is set.
              spec:
              - api_field: name
                help_text: |-
                  Specifies the name of the header in the request.
              - api_field: value
                help_text: |-
                  Specifies how the header match is performed.
                spec:
                - api_field: exact
                  one_of_index: 0
                  help_text: |-
                    Matches the input string exactly to the string specified here.

                    Examples:

                    * ``abc`` only matches the value ``abc``.
                - api_field: prefix
                  one_of_index: 0
                  help_text: |-
                    Checks if the input string has the prefix specified here.
                    Note: empty prefix is not allowed, please use regex instead.

                    Examples:

                    * ``abc`` matches the value ``abc.xyz``
                - api_field: suffix
                  one_of_index: 0
                  help_text: |-
                    Checks if the input string has the suffix specified here.
                    Note: empty suffix is not allowed, please use regex instead.

                    Examples:

                    * ``abc`` matches the value ``xyz.abc``
                - api_field: contains
                  one_of_index: 0
                  help_text: |-
                    Checks if the input string contains the substring specified here.
                    Note: empty contains match is not allowed, please use regex instead.

                    Examples:

                    * ``abc`` matches the value ``xyz.abc.def``
                - api_field: ignoreCase
                  help_text: |-
                    Indicates if the exact/prefix/suffix/contains matching should be
                    case insensitive. For example, when true, the matcher ``data`` matches
                    both input strings ``Data`` and ``data``.
          - api_field: hosts
            help_text: |-
              Specifies a list of HTTP Hosts to match against. The match can be one
              of exact, prefix, suffix, or contains (substring match). Matches are
              always case sensitive unless the ignoreCase is set. The match follows
              OR semantics which means that if any of the hosts match, the
              operation is considered to be matched.
            spec:
            - api_field: exact
              one_of_index: 0
              help_text: |-
                Matches the input string exactly to the string specified here.

                Examples:

                * ``abc`` only matches the value ``abc``.
            - api_field: prefix
              one_of_index: 0
              help_text: |-
                Checks if the input string has the prefix specified here.
                Note: empty prefix is not allowed, please use regex instead.

                Examples:

                * ``abc`` matches the value ``abc.xyz``
            - api_field: suffix
              one_of_index: 0
              help_text: |-
                Checks if the input string has the suffix specified here.
                Note: empty suffix is not allowed, please use regex instead.

                Examples:

                * ``abc`` matches the value ``xyz.abc``
            - api_field: contains
              one_of_index: 0
              help_text: |-
                Checks if the input string contains the substring specified here.
                Note: empty contains match is not allowed, please use regex instead.

                Examples:

                * ``abc`` matches the value ``xyz.abc.def``
            - api_field: ignoreCase
              help_text: |-
                Indicates if the exact/prefix/suffix/contains matching should be
                case insensitive. For example, when true, the matcher ``data`` matches
                both input strings ``Data`` and ``data``.
          - api_field: paths
            help_text: |-
              Specifies a list of paths to match against. The match can be one of
              exact, prefix, suffix, or contains (substring match). Matches are
              always case sensitive unless the ignoreCase is set. The match follows
              OR semantics which means that if any of the paths match, the
              operation is considered to be matched.
              Note that this path match includes the query parameters. For gRPC
              services, this should be a fully-qualified name of the form
              /package.service/method.
            spec:
            - api_field: exact
              one_of_index: 0
              help_text: |-
                Matches the input string exactly to the string specified here.

                Examples:

                * ``abc`` only matches the value ``abc``.
            - api_field: prefix
              one_of_index: 0
              help_text: |-
                Checks if the input string has the prefix specified here.
                Note: empty prefix is not allowed, please use regex instead.

                Examples:

                * ``abc`` matches the value ``abc.xyz``
            - api_field: suffix
              one_of_index: 0
              help_text: |-
                Checks if the input string has the suffix specified here.
                Note: empty suffix is not allowed, please use regex instead.

                Examples:

                * ``abc`` matches the value ``xyz.abc``
            - api_field: contains
              one_of_index: 0
              help_text: |-
                Checks if the input string contains the substring specified here.
                Note: empty contains match is not allowed, please use regex instead.

                Examples:

                * ``abc`` matches the value ``xyz.abc.def``
            - api_field: ignoreCase
              help_text: |-
                Indicates if the exact/prefix/suffix/contains matching should be
                case insensitive. For example, when true, the matcher ``data`` matches
                both input strings ``Data`` and ``data``.
          - api_field: methods
            help_text: |-
              Specifies a list of HTTP methods to match against. Each entry must be
              a valid HTTP method name (GET, PUT, POST, HEAD, PATCH, DELETE,
              OPTIONS). It only allows exact match and is always case sensitive.
              The match follows OR semantics which means that if any of the methods
              match, the operation is considered to be matched.
        - api_field: notDestination
          help_text: |-
            Describes the negated properties of a request's destination.
            Matches requests for destination that does not match the criteria
            specified in this field. At least one of destination or
            not_destination must be specified.
          spec:
          - api_field: headerSet
            help_text: |-
              Specifies a list of headers to match against in http header.
            spec:
            - api_field: headers
              help_text: |-
                Contains a list of headers to match against in http header.
                The match can be one of exact, prefix, suffix, or contains
                (substring match). The match follows AND semantics which means all
                the headers must match. Matches are always case sensitive unless
                the ignoreCase is set.
              spec:
              - api_field: name
                help_text: |-
                  Specifies the name of the header in the request.
              - api_field: value
                help_text: |-
                  Specifies how the header match is performed.
                spec:
                - api_field: exact
                  one_of_index: 0
                  help_text: |-
                    Matches the input string exactly to the string specified here.

                    Examples:

                    * ``abc`` only matches the value ``abc``.
                - api_field: prefix
                  one_of_index: 0
                  help_text: |-
                    Checks if the input string has the prefix specified here.
                    Note: empty prefix is not allowed, please use regex instead.

                    Examples:

                    * ``abc`` matches the value ``abc.xyz``
                - api_field: suffix
                  one_of_index: 0
                  help_text: |-
                    Checks if the input string has the suffix specified here.
                    Note: empty suffix is not allowed, please use regex instead.

                    Examples:

                    * ``abc`` matches the value ``xyz.abc``
                - api_field: contains
                  one_of_index: 0
                  help_text: |-
                    Checks if the input string contains the substring specified here.
                    Note: empty contains match is not allowed, please use regex instead.

                    Examples:

                    * ``abc`` matches the value ``xyz.abc.def``
                - api_field: ignoreCase
                  help_text: |-
                    Indicates if the exact/prefix/suffix/contains matching should be
                    case insensitive. For example, when true, the matcher ``data`` matches
                    both input strings ``Data`` and ``data``.
          - api_field: hosts
            help_text: |-
              Specifies a list of HTTP Hosts to match against. The match can be one
              of exact, prefix, suffix, or contains (substring match). Matches are
              always case sensitive unless the ignoreCase is set. The match follows
              OR semantics which means that if any of the hosts match, the
              operation is considered to be matched.
            spec:
            - api_field: exact
              one_of_index: 0
              help_text: |-
                Matches the input string exactly to the string specified here.

                Examples:

                * ``abc`` only matches the value ``abc``.
            - api_field: prefix
              one_of_index: 0
              help_text: |-
                Checks if the input string has the prefix specified here.
                Note: empty prefix is not allowed, please use regex instead.

                Examples:

                * ``abc`` matches the value ``abc.xyz``
            - api_field: suffix
              one_of_index: 0
              help_text: |-
                Checks if the input string has the suffix specified here.
                Note: empty suffix is not allowed, please use regex instead.

                Examples:

                * ``abc`` matches the value ``xyz.abc``
            - api_field: contains
              one_of_index: 0
              help_text: |-
                Checks if the input string contains the substring specified here.
                Note: empty contains match is not allowed, please use regex instead.

                Examples:

                * ``abc`` matches the value ``xyz.abc.def``
            - api_field: ignoreCase
              help_text: |-
                Indicates if the exact/prefix/suffix/contains matching should be
                case insensitive. For example, when true, the matcher ``data`` matches
                both input strings ``Data`` and ``data``.
          - api_field: paths
            help_text: |-
              Specifies a list of paths to match against. The match can be one of
              exact, prefix, suffix, or contains (substring match). Matches are
              always case sensitive unless the ignoreCase is set. The match follows
              OR semantics which means that if any of the paths match, the
              operation is considered to be matched.
              Note that this path match includes the query parameters. For gRPC
              services, this should be a fully-qualified name of the form
              /package.service/method.
            spec:
            - api_field: exact
              one_of_index: 0
              help_text: |-
                Matches the input string exactly to the string specified here.

                Examples:

                * ``abc`` only matches the value ``abc``.
            - api_field: prefix
              one_of_index: 0
              help_text: |-
                Checks if the input string has the prefix specified here.
                Note: empty prefix is not allowed, please use regex instead.

                Examples:

                * ``abc`` matches the value ``abc.xyz``
            - api_field: suffix
              one_of_index: 0
              help_text: |-
                Checks if the input string has the suffix specified here.
                Note: empty suffix is not allowed, please use regex instead.

                Examples:

                * ``abc`` matches the value ``xyz.abc``
            - api_field: contains
              one_of_index: 0
              help_text: |-
                Checks if the input string contains the substring specified here.
                Note: empty contains match is not allowed, please use regex instead.

                Examples:

                * ``abc`` matches the value ``xyz.abc.def``
            - api_field: ignoreCase
              help_text: |-
                Indicates if the exact/prefix/suffix/contains matching should be
                case insensitive. For example, when true, the matcher ``data`` matches
                both input strings ``Data`` and ``data``.
          - api_field: methods
            help_text: |-
              Specifies a list of HTTP methods to match against. Each entry must be
              a valid HTTP method name (GET, PUT, POST, HEAD, PATCH, DELETE,
              OPTIONS). It only allows exact match and is always case sensitive.
              The match follows OR semantics which means that if any of the methods
              match, the operation is considered to be matched.
      - api_field: rateLimitActions
        help_text: |-
          Specifies the actions to take when this rule is matched.
        spec:
        - api_field: rateLimitBucket
          help_text: |-
            Specifies the name of the rate limit bucket to apply when this rule
            is matched.
    - arg_name: rate-limit-buckets
      api_field: rateLimitPolicy.rateLimitBuckets
      required: false
      repeated: true
      help_text: |-
        Specifies a list of rate limit buckets to be used for rate limiting.
        Rate limit buckets will be referenced by the rate limit actions by name.
      spec:
      - api_field: name
        help_text: |-
          Specifies the name of the rate limit bucket. Name will be used to
          reference the bucket in the RateLimitAction.
      - api_field: keys
        help_text: |-
          Specifies the keys to use for rate limiting. At least one key is
          required. If multiple keys are specified, the keys will be combined and
          used as a single key.
        spec:
        - api_field: keyType
          help_text: |-
            Specifies the type of key to use for rate limiting.
        - api_field: header
          help_text: |-
            Specifies the header name if key_type is HTTP_HEADER.
        - api_field: principalType
          help_text: |-
            Specifies the principal type if key_type is PRINCIPAL.
      - api_field: cost
        help_text: |-
          Specifies the cost of the request.
        spec:
        - api_field: number
          help_text: |-
            Specifies the fixed number cost if from is NUMBER.
        - api_field: header
          help_text: |-
            Specifies the header name for cost if from is HEADER.
      - api_field: defaultLimit
        help_text: |-
          Specifies the default limit to apply for this rate limit bucket.
        spec:
        - api_field: countLimit
          one_of_index: 0
          help_text: |-
            Defines the count limit to enforce.
          spec:
          - api_field: count
            help_text: |-
              Specifies the maximum number of costs allowed in the specified
              interval. Must be non-negative.
          - api_field: interval
            help_text: |-
              Specifies the interval in units for which the count
              limit is enforced. Must be positive.
          - api_field: intervalUnit
            help_text: |-
              Specifies the unit of the interval. Defaults to MINUTES.
      - api_field: userOverrides
        help_text: |-
          Specifies a list of user overrides to apply to the rate limit bucket.
        spec:
        - api_field: overrideKey
          help_text: |-
            Specifies the key to override.
          spec:
          - api_field: sourceIp
            help_text: |-
              Specifies the source IP if the rate limit bucket keys contain a key
              of type SOURCE_IP.
          - api_field: httpHeader
            help_text: |-
              Specifies the HTTP header if the rate limit bucket keys contain a key
              of type HTTP_HEADER.
          - api_field: httpPath
            help_text: |-
              Specifies the HTTP path if the rate limit bucket keys contain a key
              of type HTTP_PATH.
          - api_field: principal
            help_text: |-
              Specifies the principal if the rate limit bucket keys contain a key
              of SOURCE_IP. PRINCIPAL.
        - api_field: limit
          help_text: |-
            Specifies the limit to apply for this specific key.
          spec:
          - api_field: countLimit
            one_of_index: 0
            help_text: |-
              Defines the count limit to enforce.
            spec:
            - api_field: count
              help_text: |-
                Specifies the maximum number of costs allowed in the specified
                interval. Must be non-negative.
            - api_field: interval
              help_text: |-
                Specifies the interval in units for which the count
                limit is enforced. Must be positive.
            - api_field: intervalUnit
              help_text: |-
                Specifies the unit of the interval. Defaults to MINUTES.
    - arg_name: request-id
      api_field: requestId
      required: false
      repeated: false
      help_text: |-
        Specifies an optional request ID to identify requests. Specify a unique
        request ID
        so that if you must retry your request, the server will know to ignore
        the request if it has already been completed. The server will guarantee
        that for at least 60 minutes since the first request.

        For example, consider a situation where you make an initial request and the
        request times out. If you make the request again with the same request
        ID, the server can check if original operation with the same request ID
        was received, and if so, will ignore the second request. This prevents
        clients from accidentally creating duplicate commitments.

        The request ID must be a valid UUID with the exception that zero UUID is
        not supported (00000000-0000-0000-0000-000000000000).
  request:
    api_version: v1alpha1
    collection:
    - networksecurity.projects.locations.rateLimitPolicies
  async:
    collection:
    - networksecurity.projects.locations.operations
